# v1.25 Live Scan Catalog — Data Matrix, ISO 15434 Format 06
**Date**: 2026-05-18 08:37:03  
**Script**: v1.25 (confirmed: `v1.25 q=r.trucheck m=found`)  
**Symbol**: 16×36 Data Matrix, ISO 15434 Format 06, MH10.8.2 DIs: 18V / 1P / S  
**Device**: DM475-63530E-PIPS-Verif-Lab, firmware 6.1.16_sr4  
**Files**:
- Push XML: `v1.25-2026-05-18-DataMatrix-DM-Live-GS1Format06.xml`
- DMST PDF: `v1.25-2026-05-18-DataMatrix-DM-Live-DMSTReport.pdf`

---

## v1.25 Field Confirmation

| Field | Push XML Value | Notes |
|---|---|---|
| PushScriptDiag | `v1.25 q=r.trucheck m=found` | CONFIRMED |
| SymbologyId | `]d1` | AIM = Data Matrix ECC 200 |
| SymbolQuality | `41` | Decoder confidence |
| SymbolAngle | `2` | degrees |
| ModuleSizePx | `16.248798370361328` | matches PDF "Pixels per Module: 16.25" |
| OpticsSource | `LiveScan` | CU=69, MRD=71 — both real values (not -1) ✓ |
| JpegImageBase64 | 9,864 chars, `/9j/4AAQSkZ...` | Valid JPEG, live DM scan |
| HorizontalBWG | `11` | matches PDF "Horizontal BWG: 11%" ✓ |
| VerticalBWG | `11` | matches PDF "Vertical BWG: 11%" ✓ |
| CalibrationDate | `1/15/2026 3:04:14 PM` | |
| FieldCalibrated | `false` | calibration needed |
| FactoryCalibrated | `false` | |
| MinPassGrade | `NA` | no pass threshold set |
| ApplicationStandard | `Custom` | |
| ApplicationPass | `Fail (Quality)` | |
| ApplicationPassReason | `Quality` | parsed from suffix ✓ |
| FPDValue | `4` | Fixed Pattern Damage numeric |
| DDGrade | `X` | Data Detail — not applicable for this type |
| AverageGrade | `A` | DMST "Average Grade" parameter #17 |
| AverageGradeNumeric | `4` | |
| ContrastUniformityRow | `12` | worst-module row |
| ContrastUniformityCol | `17` | worst-module col |
| MinReflectance | (empty) | path not yet wired (see Probe Findings) |
| DataCodewords | (empty) | not in push channel |
| ErrorCapacityUsed | (empty) | not in push channel |

---

## Probe Findings

### DebugMetricsKeys (COMPLETE KEY LIST)
`r.metrics` keys: `symbolContrast`, `cellContrast`, `axialNonUniformity`, `printGrowth`,
`UEC`, `modulation`, `fixedPatternDamage`, `gridNonUniformity`, **`extremeReflectance`**,
**`reflectMin`**

**Action for v1.26**: Wire MinReflectance from `r.metrics.extremeReflectance` or
`r.metrics.reflectMin` — one of these is the MinReflectance source. Add both to DebugRSiblings
probe to determine which path contains the percentage value (PDF shows no MinReflectance
for this scan but other scans may have it).

### DebugSymbols0: `(q.symbols[0] null)`
`r.trucheck.symbols[0]` is null/undefined for a DM live scan.
**Hypothesis**: `symbols[]` array may be QR-only (QR has multiple version/format
info blocks that map to a "symbols" concept). For DM, grade structure is flat under
`q.gradeResult` or `q.general`. Must probe a live QR scan to confirm.
**Action for v1.26**: Run on a live QR scan; also probe `Object.keys(r.trucheck)` to
enumerate all top-level trucheck properties.

### DebugPrintGrowth: `raw=-1;grade=NA;`
`r.metrics.printGrowth.raw = -1` — the printGrowth metric is -1 for this scan.
BUT `HorizontalBWG=11` and `VerticalBWG=11` are correctly populated from `q.general`.
**Conclusion**: `r.metrics.printGrowth` is not the BWG source for the push channel.
BWG comes from `q.general.horizontalBWG` / `q.general.verticalBWG` ✓.
`BWGPercent` (singular, legacy) via `r.metrics.printGrowth` will always be empty — can
be removed from schema in future cleanup. H/V split is the correct representation.

### DebugImageShape (WORKING — was silent in v1.24)
`id=0;index=0;FoV=[obj];RoI=[obj];exposureTime=32;gain=1.00;autoExposure=true;
illEnabled=true;illIntensity=1;extIllEnabled=true;extIllIntensity=1;focusLength=0;
focusPower=0.00;setupIndex=0;inputStates=...`
Image shape probe now returns data (v1.24 had a silent JS error; v1.25 fixed with try/catch).
Imaging params: exposure=32ms, gain=1.00, autoExposure=on, illumination=on (intensity=1),
external illumination=on.

### DebugGS1: All `undefined`
All GS1 AIs are undefined. Expected — Data Format Check = Custom (ISO 15434), not GS1.
The symbol content IS ISO 15434 with MH10.8.2 DIs (18V, 1P, S), not standalone GS1 AIs.
GS1 AIs would only be present if Data Format Check = GS1 mode.

### DebugDodUid: All `undefined`
No DoD UID encoding present. Expected for this symbol.

### DebugBarcodeAsgn: `result=-1`
Barcode assignment not configured. Expected.

---

## AG Discrepancy — Confirmed

| Source | Value | Grade | Formula |
|---|---|---|---|
| Push XML `AGValue` | 10.5 | B | Unclear — device's own AG computation |
| Push XML `AverageGrade` / `AverageGradeNumeric` | A / 4 | A | DMST "Average Grade" parameter #17 |
| DMST PDF parameter #17 | 4.0 | A | PASS |

**Conclusion**: `AGValue=10.5 → B` is the device's internal average-grade calculation
(presumably mean of raw grade numerics using a 0–16 scale?). `AverageGrade=A/4` matches
the DMST PDF exactly and is the value VTCCP should report. Both are stored in VerificationRecord.

---

## DMST PDF Key Findings

### Encodation Analysis Section — CODEWORD TABLE EXISTS IN DEVICE
The PDF shows a full codeword-by-codeword decode table:
- Each DM codeword value (092, 042, 063...) → mode (ASCII) → decoded character
- All 32 data codewords + pad codewords + 24 ECC codewords
- `*` = error-corrected codeword (7 errors corrected, matching ErrorsCorrected=7)

**THIS DATA COMES FROM THE DEVICE** (DMST must be pulling it via DMCC — the device knows
which codewords were error-corrected, so it cannot be re-computed from the decoded string).
**Implication for F1 (codeword decoder)**: No third-party SDK needed for the codeword table.
The device exposes it. B7 must identify the DMCC command DMST uses to pull this.
**Implication for Dynamsoft**: Probably NOT needed for VTCCP's use case. The device is
the authoritative source.

**Data codewords** (32, with `*` = error-corrected):
`092 042 063 031 136 030 148 087 *069 *219 *183 *055 030 050 081 219 132 *066 *030 *084 161 152 066 132 226 054 031 005 129 045 195 090`

**ECC codewords** (24):
`107 149 089 060 057 044 200 195 082 134 017 211 227 021 091 158 239 024 033 160 108 040 223 204`

### ASCII Values Row
`091 041 062 030 048 054 029 049 056 086 068 056 057 053 051 054 029 049 080 056 057 048 050 065 029 083 051 049 050 050 065 048 050 057 054 053 030 004`
Decodes to: `[)><RS>06<GS>18VD89536<GS>1P8902A<GS>S3122A02965<RS><EO>`
Matches `DecodedData` in push XML exactly ✓

### Modulation Values Grid
18 rows × 38 cols = 684 values (16×36 DM + 1-module QZ per side = 18×38) ✓
Range: 54–99 for live scan. Zeros at specific positions = worst-contrast modules.
Matches `DebugModSize: len=684` ✓.
Source: must be a DMCC pull (same command as codeword data, presumably).

### EncodedCharacters Discrepancy
- Push XML: `EncodedCharacters: 33`
- DMST PDF: `Encoded characters: 38`
- Confirmed different counting conventions (push counts differently than DMST display).
  DMST likely counts all characters including control chars; push may count data chars only.

### TQZ / RQZ / TTR / RTR / TCT / RCT = X in push, A in DMST
Push XML has `TQZGrade: X`, `RQZGrade: X`, `TTRGrade: X`, `RTRGrade: X`, `TCTGrade: X`,
`RCTGrade: X`. But DMST PDF shows parameters 11a/11b (ULQZ/URQZ), 12a/12b (RUQZ/RLQZ),
13-14 (TTR), 15-16 (TCT) all as A (4.0 PASS). Gap in push channel — these grades are
available (DMST shows them) but not accessible via current push paths. Probe target for v1.26.

---

## Action Items for v1.26

| Priority | Action | Why |
|---|---|---|
| HIGH | Probe `Object.keys(r.trucheck)` to find where ULQZ/URQZ/RUQZ/RLQZ/TTR/TCT grade data lives | Currently X in push but A in DMST — data is there, path unknown |
| HIGH | Probe `r.metrics.extremeReflectance` and `r.metrics.reflectMin` shapes | MinReflectance wire fix |
| HIGH | Run DebugSymbols0 on a live QR scan | Confirm whether `r.trucheck.symbols[0]` is QR-only |
| MED | Probe `r.trucheck` top-level keys | Systematic enumeration — avoid guessing paths |
| LOW | Wire `DataCodewords` and `ErrorCapacityUsed` if accessible | Currently empty in push; available in DMST (32 / 14%) |
