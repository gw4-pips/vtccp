# Scan #16 Catalog — v1.36 Device Confirmed

**Script**: v1.36 — DEVICE CONFIRMED ✓  
**Confirm tag**: `<PushScriptDiag>v1.36 q=r.trucheck m=found</PushScriptDiag>`  
**Date/Time**: 2026-06-09T20:38:45  
**Device**: DM475-63530E-PIPS-Verif-Lab  
**Symbol**: Data Matrix ECC200 22×22, live scan  
**AIM ID**: `]d2` → GS1 DataMatrix (FNC1 first position)  
**Grade**: **F** (OverallGrade=F, FormalGrade=0/F, GradingStandard=**ISO 15415:2024**)  
**XML**: `v1.36-2026-06-09-DM-22x22-GradeF-GS1-4AI.xml`

---

## §1. ★ NEW — GradingStandard = ISO 15415:2024

All previous 15 scans reported `ISO 15415:2011`. This scan reports **ISO 15415:2024** — the device
was recalibrated on 6/8/2026 and the new calibration changed the edition string.

**Design rule confirmed**: `GradingStandard` (and `Standard`) must be read from the push XML and
stored verbatim — never hardcoded. Both fields present and identical in this scan.

CalibrationDate: **6/8/2026 5:25:23 PM** (fresh calibration, 1 day before this scan)

---

## §2. v1.36 Probe Results

### DebugJpegDiag
```
q.jpegImage=str.len=15532 | r.jpegImage=null | qImgKeys=jpegImage=str.len=15532
```
**JPEG IS PRESENT** — 15,532 chars of base64 in `q.jpegImage`. `r.jpegImage` does not exist (null).  
`<JpegImageBase64>` element confirmed present in the XML (len=15,532 chars).  
The earlier "regression" was a VTCCP parser issue, not a script/firmware issue. Image path = `q.jpegImage`.

### DebugQTopKeys — Complete r.trucheck key inventory (50 keys)

Full key list with notable items:

| Key | Value / Type | Notes |
|---|---|---|
| `UII` | `str.len=0` | **NEW** — UII field present; empty here (GS1, not MIL-STD-130). Will populate on UID items. |
| `batch` | `str.len=7` | "1197170" — lot number confirmed ✓ |
| `calibrationDate` | `str.len=19` | Full ISO calibration date string |
| `applicationStdArray` | `[arr.13]` | **NEW** — GS1 parse results; 13 entries for this 4-AI code. Sub-keys unprobed — v1.37 candidate. |
| `asciiArray` | `[arr.48]` | **NEW** — ASCII representation; 48 chars = content length. Sub-keys unprobed. |
| `distributedDamageGrade` | `[obj]` | **NEW** — never seen before. Sub-keys unprobed. |
| `codewordArray` | `[arr.50]` | 50 = TotalCodewords ✓ |
| `modulationArray` | `[arr.576]` | 576 = 24×24 = (22+1QZ)×(22+1QZ) ✓ |
| `encodationAnalysisArray` | `[arr.31]` | EncodedCharacters per push = 31 |
| `jpegImage` | `str.len=15532` | JPEG confirmed here |
| `reflectanceLight` | 64 | RL=64 → matches SCRlRd=64/3 ✓ |
| `reflectanceDark` | 3 | Rd=3 ✓ |
| `customNote` | `str.len=11` | "CUSTOM NOTE" (11 chars) — first populated instance |
| `linearEdge`..`linearQuietZone` | `[obj]` | 9 linear (ISO 15416) grade stubs — empty on DM, present in schema |
| `alignmentPatterns` | `[obj]` | Likely QR-only; stub present on DM |
| `versionInformationBlock` | `[obj]` | Confirmed present on DM (QR-only grades — will be empty sub-keys) |
| `formatInformationBlock` | `[obj]` | Same |

### DebugRTopKeys
```
decoded=true; content=str.len=48; decodeTime=455; triggerTime=493; timeout=2000;
readSetup=0; symbology=[obj]; image=[obj]; validation=[obj]; metrics=[obj];
source=str.len=27; annotation=str.len=0; label=str.len=0; custom_svg=str.len=0;
trucheck=[obj]; barcodeAssignment=[obj]
```
Consistent with scan #13 inventory (16 keys). `content=str.len=48` matches `asciiArray=[arr.48]`.

### DebugRImageRoI
```
left=360; top=584; right=2032; bottom=1348
```
ROI = 1672×764 pixel crop of the 2448×2048 full frame (symbol centred in the field).  
**These values ARE the DECODER.ROI equivalents.** VTCCP can read ROI directly from push XML —
no separate `DMCC GET DECODER.ROI` call needed on each scan.  
These coordinates are per-scan (symbol-dependent), not constant.

### DebugRImageFoV
```
left=0; top=0; right=2448; bottom=2048
```
Full sensor frame = 2448×2048 — matches DM475V hardware spec ✓.  
This value is **constant** for a given device. Safe to read once on connect and cache.  
Confirms: `r.image.FoV` = sensor extent; `r.image.RoI` = per-scan barcode crop.

### DebugBarcodeAssignment
```
result=-1; stats=[obj]
```
Two sub-keys: `result` (int) and `stats` (object). `result=-1` = no assignment active (sentinel).  
`stats` sub-keys unprobed — queue for v1.37 if relevant.

---

## §3. GS1 Data Decode

**DecodedData**: `010035551371021321100003289347<GS>17280331101197170`  
**AIM ID**: `]d2` → GS1 DataMatrix (FNC1 first position, standard GS1 encoding)

| AI | Value | Meaning |
|---|---|---|
| `01` | 00355513710213 | GTIN-14 (pharmaceutical/healthcare item) |
| `21` | 100003289347 | Serial Number |
| `17` | 280331 | Expiry / Best Before: 2028-03-31 |
| `10` | 1197170 | Lot / Batch Number |

`ApplicationStandard=Custom`, `ApplicationPass=Fail (Quality)` → GS1 format is valid;
overall grade F is a quality failure, not a data format failure.

---

## §4. Grade Summary (22×22 ECC200, Grade F)

| Field | Value | Notes |
|---|---|---|
| OverallGrade | **F** | |
| FormalGrade | 0/F | |
| DecodeGrade | **F** | Primary failure driver |
| FPDGrade | **D** | Fixed Pattern Damage = 0.6 |
| TCTGrade | **D** | Top Clock Track |
| MODGrade | C | |
| RMGrade | C | |
| SCGrade | B | SC=61.1%, RL=64, Rd=3 |
| UECGrade | A | 70% unused |
| ANUGrade | A | 5.4% |
| GNUGrade | A | 34.1% (push raw; HTML will show ~0.34%) |
| LLSGrade/BLSGrade | A / A | L-side grades |
| All QZ grades | A | LQZ/BQZ/TQZ/RQZ |
| TTR/RTR | A / A | |
| RCTGrade | A | |
| AverageGrade | C / 2.4 | |
| NominalXDim | 16.0 mil | |
| MatrixSize | 22×22 | |
| EncodedCharacters | 31 | (push value; HTML authoritative) |
| TotalCodewords | 50 | DataCW=30, ECBudget=20 (calc from UEC=70%, ECUsed=6) |
| DataCodewords | *empty* | C# table: 30 for 22×22 ECC200 |
| ErrorCorrectionBudget | *empty* | C# table: 20 for 22×22 ECC200 |
| ErrorsCorrected | 3 | |
| ErrorCapacityUsed | 6 | = 3×2 ✓ |
| OpticsSource | LiveScan | CU=7, MRD=6 ✓ |
| ImagePolarity | *empty* | Still not in push XML — HTML scraper required |
| CustomNote | CUSTOM NOTE | First populated instance in live scans |
| JpegImageBase64 | 15,532 chars | JPEG confirmed present ✓ |
| CalibrationDate | 6/8/2026 5:25:23 PM | Fresh recalibration |
| FieldCalibrated | false | |

---

## §5. New XML Elements (empty on DM, in schema)

These appear in the element list but are empty in this DM scan.
They will populate on 1D (ISO 15416) scans:

| Element | Meaning |
|---|---|
| `SymbolAnsiGrade` | ANSI grade (as opposed to ISO grade) |
| `BWGPercent` | Combined bar/space width growth percent |
| `Magnification` | 1D X-dimension magnification factor |
| `NominalXDim1D` | 1D X-dimension in mils (parallel to NominalXDim for 2D) |
| `Ratio` | 1D bar/space ratio |
| `AvgSC`, `AvgMOD`, `AvgDefect`, `AvgEdge` | ISO 15416 scan reflectance profile averages |
| `AvgDEC`, `AvgDcod` | Average decode and decodability grades |
| `AvgHQZ`, `AvgLQZ`, `AvgRQZ`, `AvgMinQZ` | Average quiet zone measurements |
| `AvgMinEC` | Average minimum edge contrast |
| `AvgRlRd` | Average Rl/Rd (light/dark reflectance) |

These confirm the push script schema covers full ISO 15416 repeatability averaging.
VTCCP VerificationRecord and ISO15416Mapper need these fields added.

---

## §6. Codeword Math — 22×22 ECC200

From UECPercent=70% and ErrorCapacityUsed=6:
- ECBudget = 6 / (1 - 0.70) = **20 codewords**
- DataCodewords = 50 - 20 = **30 codewords**
- Formula check: UEC = (20-6)/20 = 70% ✓
- ErrCapUsed formula: 3 errors × 2 = 6 ✓

Add `{22, 22, 30, 20}` row to C# ECC200 lookup table.

---

## §7. Open probes for v1.37

| Probe | Target | Purpose |
|---|---|---|
| `DebugApplicationStdArray` | `q.applicationStdArray[i]` keys | What GS1 parser fields are in each entry — this is the structured AI parse result |
| `DebugAsciiArray` | `q.asciiArray[i]` | ASCII decoded content structure |
| `DebugDistributedDamage` | `q.distributedDamageGrade` sub-keys | New grade type — what does it measure? |
| `DebugBarcodeAssignmentStats` | `r.barcodeAssignment.stats` sub-keys | What stats are tracked? |

Priority: `DebugApplicationStdArray` — this is the structured GS1 parse result array and may
expose AI field values directly from the firmware without needing the gs1-syntax-engine.

