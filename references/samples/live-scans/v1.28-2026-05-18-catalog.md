# v1.28 Live Scan Catalog — 2026-05-18

Push script: `DmstPushScript_v1.28.js`  
Device: DM475-63530E-PIPS-Verif-Lab (10.10.10.7, fw 6.1.16_sr4)  
Confirm tag: `<PushScriptDiag>v1.28 q=r.trucheck m=found</PushScriptDiag>` ✓

---

## Scan 1 — DM Live, GS1 Format 06

**File:** `v1.28-2026-05-18-DM-Live-GS1Format06.xml`  
**DateTime:** 2026-05-18T11:24:40

### Symbol identity

| Field | Value |
|---|---|
| Symbology | Data Matrix (ECC200) |
| MatrixSize | 16×36 |
| DecodedData | `[)>RS06GSVD89536GS1P8902AGS3122A02965RSEOT` (GS1 Format 06) |
| SymbologyId | `]d1` |
| SymbolQuality | 41 |
| SymbolAngle | 2° |
| ModuleSizePx | 16.28 px |

### Grading summary

| Field | Value | Note |
|---|---|---|
| OverallGrade | D (1) | Fail |
| FormalGrade | 1/D | |
| ApplicationPass | Fail (Quality) | |
| ApertureRef | 17 | |
| Wavelength | 660 nm | |
| Lighting | 45Q | |
| Standard | ISO 15415:2011 | |

### Per-parameter grades

| Parameter | Value | Grade |
|---|---|---|
| AverageGrade | 10.3 | B |
| UEC | 41.7% | C |
| SC | 87.8% | A |
| SCRlRd | 92/4 | — |
| MinReflectance | **4** | — |
| MOD | — | A |
| RM | — | C |
| ANU | 11.3% | **D** ← limiting |
| GNU | 5.5% | A |
| FPD | 4 | A |
| Decode | — | A |
| LLS | — | A |
| BLS | — | A |
| LQZ | — | A |
| BQZ | — | A |
| TQZ | — | X |
| RQZ | — | X |
| TTR | — | X |
| RTR | — | X |
| TCT | — | X |

### Codeword / error correction

| Field | Value |
|---|---|
| TotalCodewords | 56 |
| DataCodewords | *(empty — q.symbols not in push channel)* |
| ErrorCorrectionBudget | *(empty — q.symbols not in push channel)* |
| ErrorsCorrected | 7 |
| ErrorCapacityUsed | **14** (= 7 × 2 ✓) |
| ErrorCorrectionType | ECC200 |
| EncodedCharacters | 33 |

### Metadata

| Field | Value |
|---|---|
| ContrastUniformity | 70 |
| MRD | 71 |
| ContrastUniformityRow | 12 |
| ContrastUniformityCol | 17 |
| HorizontalBWG | 11 |
| VerticalBWG | 11 |
| NominalXDim | 21.5 mil |
| OpticsSource | LiveScan |
| CalibrationDate | 1/15/2026 3:04:14 PM |
| FieldCalibrated | false |
| FactoryCalibrated | false |

---

## Wire Fix Confirmations

### MinReflectance → `q.reflectanceDark` ✓
**v1.28 emits `<MinReflectance>4</MinReflectance>`** (Rd = 4%).  
v1.27 emitted empty string — wire was broken, `q.minimumReflectance` is always `{raw:0,grade:F}` firmware sentinel.  
v1.28 fix: `q.reflectanceDark` (primitive) → `_rdRaw = 4` → `Math.round(_refToPct(4))` = 4 ✓.

### ErrorCapacityUsed → `_ecCount × 2` ✓
**v1.28 emits `<ErrorCapacityUsed>14</ErrorCapacityUsed>`** (7 corrected × 2 = 14).  
v1.27 emitted empty — formula was unconfirmed.  
v1.28 fix: `_ecCount * 2` = 7 × 2 = 14 ✓.

---

## New Probe Results

### DebugArrayLens
```
modLen=684; cwLen=56; eaLen=33
```

| Array | Length | Interpretation |
|---|---|---|
| `modulationArray` | **684** | = (16+2) × (36+2) = **18×38** — full graded region **including quiet zone border** |
| `codewordArray` | **56** | = **total codewords (data + ECC combined)** — matches `TotalCodewords=56` ✓ |
| `encodationAnalysisArray` | **33** | = **encoded characters** — matches `EncodedCharacters=33` ✓ |

**B7 architecture implications:**
- ModValues sheet grid is **18 rows × 38 cols** (NOT 16×36) — QZ border included
- For DataCodewords derivation from push: `TotalCodewords − ECBudget` — but ECBudget still NOT in push. For 16×36 ECC200: 56 total − 24 ECC = **32 data codewords** (can be derived from matrix size lookup table internally)
- `encodationAnalysisArray` is indexed per encoded character (33 entries), NOT per codeword
- `DataCodewords` and `ErrorCorrectionBudget` remain empty — `q.symbols` path not present on firmware 6.1.16_sr4 → v1.29 `DebugSymbols` probe still needed

### DebugModGradeN
```
grade=(; gradeLen=1; raw=94; isBlack=1
```

| Field | Value | Interpretation |
|---|---|---|
| grade | `(` | Single ASCII character — **NOT truncation** (gradeLen=1 confirms) |
| gradeLen | 1 | Confirmed: grade field is a 1-char string |
| raw | 94 | Reflectance raw value (0–255 scale). 94/255 ≈ 37% reflectance for a nominally black module |
| isBlack | 1 | Module is expected to be dark (black cell) |

**Notes:** The `(` character (ASCII 0x28 = 40) is the firmware's internal per-module grade encoding. This is not the standard A–F grade scale. B7 `ModValuesSheetWriter` should store the raw reflectance value (`raw` field) and `isBlack` flag as the primary data columns; the `grade` field can be stored as-is for reference.

---

## QR Sentinel Grades on DM Scan

All QR-specific parameters emit `F` on a DataMatrix scan (correct behavior):

`ULPGrade=F, URPGrade=F, LLPGrade=F, HCTGrade=F, VCTGrade=F, ALPGrade=F, VIBGrade=F, FIBGrade=F`

This is the expected sentinel. Parser must not flag these as real QR failures on a DM symbol.

---

## Outstanding After v1.28

| Gap | Status | Next step |
|---|---|---|
| DataCodewords | Empty in push | v1.29 `DebugSymbols` probe → `q.symbols[0].dataCodewords` |
| ErrorCorrectionBudget | Empty in push | v1.29 `DebugSymbols` probe → `q.symbols[0].ecCodewords` |
| QR ErrorCorrectionType (L/M/Q/H) | "QR" placeholder | v1.29 `DebugSymbols` + `DebugValidationGS1` probes |
| Live QR optical scan | Not yet | All QR grade wires confirmed structurally; need live optical scan |
